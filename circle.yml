general:
  branches:
    ignore:
      - gh-pages

machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - mvn -s settings.xml -P production dependency:resolve

test:
  override:
    - mvn -s settings.xml clean integration-test site

deployment:
  develop:
    branch: develop
    commands:
      - mvn -s settings.xml clean deploy
  production:
    branch: master
    commands:
      - mvn -s settings.xml clean