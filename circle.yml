general:
  branches:
    ignore:
      - gh-pages

machine:
  java:
    version: oraclejdk8

test:
  override:
    - mvn -s settings.xml integration-test site

deployment:
  production:
    branch: master
    commands:
      - mvn -s settings.xml -P production site